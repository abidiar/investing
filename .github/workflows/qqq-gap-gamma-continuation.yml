name: QQQ Gap Gamma Continuation Research

on:
  push:
    branches: [main]
    paths:
      - 'research/run_qqq_gap_gamma_continuation.py'
      - 'research/qqq_gap_gamma_continuation_v1.md'
      - '.github/workflows/qqq-gap-gamma-continuation.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  backtest:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout Investing OS
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install research dependencies
        run: pip install pandas numpy scipy yfinance tabulate

      - name: Run frozen gap-conditioned gamma continuation test
        run: |
          mkdir -p research/results
          python research/run_qqq_gap_gamma_continuation.py | tee research/results/qqq_gap_gamma_continuation_v1_run.log

      - name: Commit reproducible results
        run: |
          git config user.name 'Investing OS Research Bot'
          git config user.email 'actions@users.noreply.github.com'
          git add research/results/qqq_gap_gamma_continuation_v1_*.csv research/results/qqq_gap_gamma_continuation_v1_*.md research/results/qqq_gap_gamma_continuation_v1_run.log
          if git diff --cached --quiet; then
            echo 'No result changes to commit.'
          else
            git commit -m 'Record frozen QQQ gap gamma continuation v1 results [skip ci]'
            git push
          fi
